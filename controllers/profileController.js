const asyncHandler = require("express-async-handler");
const User = require("../models/userModel")

const UserProfileView = asyncHandler(async (req,res) => {
    
})

const AuthorProfileView = asyncHandler(async (req,res) => {

})